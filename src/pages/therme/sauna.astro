---
import PageLayout from '@/layouts/PageLayout.astro';
import PageHero from '@/components/common/PageHero.astro';
import saunaData from '@/content/pages/therme-sauna.json';

const page = saunaData;

const saunaTypes = [
  { name: 'Finnische Sauna', temp: '100 °C' },
  { name: 'Biosauna', temp: '60 °C' },
  { name: 'Dampfbad', temp: '55 °C' },
  { name: 'Erlebnissauna', temp: '80 °C' },
  { name: 'Infrarotkabine', temp: '60 °C' },
  { name: 'Außensauna', temp: '90 °C' }
];
---

<PageLayout title={page.seo_title} description={page.seo_description}>

  <PageHero
    image={page.hero_image}
    imageAlt={page.hero_image_alt}
    caption={page.hero_caption}
    heading={page.hero_heading}
    intro={page.hero_intro}
    breadcrumb={[{ label: 'Start', href: '/' }, { label: 'Therme', href: '/therme' }, { label: 'Saunalandschaft' }]}
  />

  <!-- ── Intro Section ── -->
  <section class="py-16 lg:py-24 bg-cream">
    <div class="max-w-screen-xl mx-auto px-6">
      <div class="max-w-3xl mx-auto">
        <p class="caption-label mb-4 text-gold">Wärmetherapie</p>
        <h2 class="font-display font-light text-ink-dark" style="font-size:clamp(1.75rem,3.5vw,3rem);">{page.intro_heading}</h2>
        <p class="text-xs text-ink-muted mt-6 leading-relaxed">{page.intro_body}</p>
      </div>
    </div>
  </section>

  <!-- ── Sauna Types Grid ── -->
  <section class="py-16 lg:py-24 bg-stone-light/30">
    <div class="max-w-screen-xl mx-auto px-6">
      <div class="grid md:grid-cols-3 gap-8 mb-8">
        {saunaTypes.map((sauna) => (
          <div class="bg-white border border-stone-light p-8 lg:p-10 text-center">
            <h3 class="font-display font-light text-ink-dark text-lg mb-3">{sauna.name}</h3>
            <div class="flex items-center justify-center gap-2 mb-4">
              <span class="text-3xl font-display font-light text-gold">{sauna.temp}</span>
            </div>
            <p class="text-xs text-ink-light">Optimale Wärmeentwicklung</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ── Rules & Info Bar ── -->
  <section class="py-12 bg-forest-700/95 border-y border-forest-600">
    <div class="max-w-screen-xl mx-auto px-6">
      <div class="grid md:grid-cols-2 gap-8">
        <div>
          <p class="text-cream/60 text-xs tracking-widest uppercase mb-2">Saunaregeln</p>
          <p class="text-cream/80 text-xs leading-relaxed">Bitte beachten Sie unsere <a href="#" class="text-gold hover:text-gold/80 transition-colors underline">Saunaordnung</a>. Mindestalter: 16 Jahre, Begleitung Unter-16-Jähriger erforderlich.</p>
        </div>
        <div>
          <p class="text-cream/60 text-xs tracking-widest uppercase mb-2">Aufgusszeiten</p>
          <p class="text-cream/80 text-xs leading-relaxed">Regelmäßige Aufgusszeiten mit verschiedenen Düften und Kräutern, schauen Sie an der Rezeption nach dem genauen Plan.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ── Bottom CTA ── -->
  <section class="py-16 bg-white border-t border-stone-light">
    <div class="max-w-screen-xl mx-auto px-6 text-center">
      <h2 class="font-display font-light text-ink-dark" style="font-size:clamp(1.5rem,3vw,2.5rem);">Bereit für Ihren Aufenthalt?</h2>
      <p class="text-xs text-ink-muted mt-3 mb-8">Prüfen Sie die Verfügbarkeit oder kontaktieren Sie uns direkt.</p>
      <div class="flex flex-wrap gap-4 justify-center">
        <a href="#booking" class="btn-gold">Jetzt buchen</a>
        <a href="/service-kontakt" class="btn-outline-gold">Kontakt & Anfrage</a>
      </div>
    </div>
  </section>

</PageLayout>
